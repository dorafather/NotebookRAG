# NotebookRAG 1.2.2 릴리즈 노트

## 요약
색인 스캔이 파일 목록을 도는 동안 CPU 코어 하나를 거의 독점(99% 점유)하던 문제를 고쳤습니다. 실제 동작(스캔 결과)은 동일하고, OS 스케줄러가 다른 프로세스에도 시간을 나눠줄 수 있도록 스로틀링만 추가했습니다.

## 배경 — 어떻게 발견됐나
1.2.1의 검색 지연 원인을 추적하던 중, 감시 폴더의 파일을 재귀적으로 훑는 `scan_files()`가 `sorted(root.rglob("*"))`로 디렉터리 트리 전체를 한 번에 리스트로 만들어 정렬한 뒤, 파일마다 `.stat()`을 부르는 동안 **중간에 한 번도 쉬지 않는 단일 루프**라는 걸 확인했습니다. 이 루프는 30초마다(변경 여부와 무관하게) 무조건 돌아 실제 서비스 규모(수천 파일)에서 CPU 한 코어를 몇 초~몇십 초씩 계속 붙잡고 있었습니다.

## 원인
- `sorted(root.rglob("*"))`는 정렬을 위해 제너레이터 전체를 먼저 메모리에 리스트로 통째로 소비 — 이 시점부터 이미 중단 지점이 없는 통짜 순회가 됩니다.
- `scan_files()`가 반환하는 값은 상대경로를 키로 쓰는 `dict`라 순서가 결과에 전혀 영향을 주지 않으므로,애초에 정렬이 불필요했습니다.

## 수정 내용
`src/rag_indexing.py`의 `scan_files()`:
- 불필요한 `sorted()` 제거 — 제너레이터를 그대로 순회.
- 100개 항목마다 10ms 짧게 쉬어(`SCAN_YIELD_EVERY`/`SCAN_YIELD_SEC`, 환경변수로 조정 가능) 다른 프로세스가 CPU 타임슬라이스를 받을 틈을 만듦.

## 검증
실제 감시 폴더(3개, 5620개 파일)로 `time.process_time()`을 이용해 CPU 시간 자체를 직접 측정:

| | 소요시간(wall) | CPU 사용시간 | CPU 점유율 |
|---|---|---|---|
| 수정 전 | 7.72초 | 7.66초 | **99.2%** |
| 수정 후 | 17.16초 | 7.22초 | **42.1%** |

총 CPU 작업량은 동일(낭비 없음) — 같은 일을 짧게 몰아서 하던 걸 더 긴 시간에 나눠서 하도록 바꾼 것뿐입니다. 스캔 도중 색인 자식 프로세스 자신의 `/indexer/status` 응답성도 함께 확인(0.5~1.7초, 끊김 없음).

## 변경 파일
- `src/rag_indexing.py` — `scan_files()` 스로틀링
- `src/app_paths.py` — 버전 1.2.1 → 1.2.2

## 커밋
- `eff207b` Throttle scan_files() to avoid monopolizing a CPU core
