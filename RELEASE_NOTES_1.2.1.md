# NotebookRAG 1.2.1 릴리즈 노트

## 요약
검색이 12~110초까지 느려지고, 그 여파로 `/health`까지 막혀 워치독이 오탐 재시작을 일으킬 수 있었던 버그를 수정했습니다. 이번 릴리즈는 **버그 수정 전용**입니다(신규 기능 없음).

## 배경 — 어떻게 발견됐나
재부팅 후 MCP 연동(`doc_search`)이 응답하지 않는다는 보고에서 시작했습니다. 조사 과정:

1. MCP 브릿지가 `처리 실패: `(빈 메시지)로 실패 — 브릿지의 15초 고정 타임아웃이 걸린 것으로 확인.
2. 브릿지를 거치지 않고 직접 `/rag/search`를 호출해도 **40~111초**가 걸림 — 검색 자체가 느림.
3. 검색이 도는 동안 동시에 `/health`를 폴링해보니 **`/health`도 응답 불가** — 검색 한 건이 전체 프로세스를 막고 있었음.
4. 실측 중 실제로 트레이 워치독이 발동해 `notebookrag.exe`가 재시작됨(PID 변경으로 확인) — 정상 동작 중에도 이 버그가 오탐 재시작을 유발할 수 있음을 실증.
5. 로그를 자세히 보니 검색/헬스체크 요청과 무관하게, **30초마다 무조건 전체 16만 벡터 인덱스를 다시 여는** 백그라운드 루프가 있었음(`_refresh_search_index_periodically`, `notebookrag_main.py`).
6. 격리 상태로 같은 재연결 시퀀스를 직접 재보니 0.5초밖에 안 걸림 — 즉 재연결 코드 자체의 비용이 아니라, **색인 자식 프로세스가 같은 WAL 모드 sqlite 파일에 쓰기 작업 중일 때 부모가 새 커넥션을 열려다 걸리는 프로세스 간 락 경합**이 진짜 원인이었음.

## 원인
`_refresh_search_index_periodically()`는 "이미 있는 DB를 그냥 여는 가벼운 동작"이라는 가정 하에 30초마다 무조건 `open_existing_index()`를 호출해 인덱스를 다시 열었습니다. 코퍼스가 작을 때는 실제로 가벼웠지만, 지금 규모(16만 1007개 벡터, 5620개 파일)에서는:

- 이 재연결이 색인 자식 프로세스의 쓰기 작업과 같은 DB 파일을 두고 경합하면서 지연이 발생하고,
- 그 지연 동안 GIL/이벤트루프가 묶여 `/health`를 포함한 모든 요청이 멈추고,
- 충분히 길어지면 워치독의 5분 무응답 임계값을 넘겨 프로세스가 (불필요하게) 재시작됨.

## 수정 내용
`notebookrag_main.py`의 재연결 루프가 **실제로 인덱스가 바뀐 경우에만** 다시 열도록 변경:

- 활성 세대(a/b)가 바뀔 때만 감지하는 방식은 안 됨 — 평소 증분 갱신(`force=False`)은 세대를 안 바꾸고 같은 파일에 그대로 쓰기 때문(`build_index()` 확인).
- 대신 활성 세대 DB 파일의 **mtime**을 확인(`os.stat()` 한 번, 사실상 공짜)해서, 마지막으로 확인한 시점과 다를 때만 무거운 `open_existing_index()`를 호출.
- 아무것도 안 바뀐 대부분의 주기에는 재연결을 건너뛰어, 락 경합이 벌어질 기회 자체를 없앰.

색인 자식 프로세스 쪽에도 같은 패턴(`_finalize_index()`)이 있어 처음엔 "쌍둥이 버그"로 의심했으나, 격리 측정 결과 그쪽은 `ChunkStore` 객체 생성뿐이라 사실상 비용이 0이라 별도 수정 불필요로 확인됨.

## 검증
실제 프로덕션 데이터(16만 1007개 벡터)로 확인:

| 항목 | 수정 전 | 수정 후 |
|---|---|---|
| 검색 응답 시간(모델 워밍 후) | 40~111초 | 8.5초 |
| 검색 중 `/health` 응답 | 3~4초 타임아웃 3연속 | 검색 직후 즉시 0.25~0.36초 |
| 30초 주기 후 재연결 로그 | 매번 출력 | 변경 없으면 안 찍힘(확인됨) |

남은 ~8~21초(첫 요청은 모델 콜드 로드 포함)는 이번 버그와 무관한 별개 항목(순수 임베딩 계산 비용)으로, 이번 릴리즈의 수정 대상이 아닙니다.

## 변경 파일
- `src/notebookrag_main.py` — 재연결 조건 추가(mtime 비교)
- `src/app_paths.py` — 버전 1.2.0 → 1.2.1

## 커밋
- `e96c129` Fix search hang caused by unconditional periodic index reload
- `3aa620d` Bump version to 1.2.1 — search hang fix release

## 배포
`notebookrag.exe` / `mcp-rag.exe` / `tray.exe` 모두 1.2.1로 재빌드·배포·재기동 완료, `/health`로 버전 반영 확인됨.
