{
  "_설명": "이 파일은 템플릿입니다. 실제 값을 채운 사본(settings.json)은 %APPDATA%\\NotebookRAG\\ 에 저장되며, 이 config\\ 폴더(설치 경로)에는 절대 실제 값을 두지 않습니다 — 개발용 .env에 실제 API 키가 평문으로 있던 것과 같은 실수를 배포판에서 반복하지 않기 위함입니다.",

  "RAG_DOCS_DIR": "",
  "_RAG_DOCS_DIR_설명": "색인할 문서 폴더 경로. 콤마로 여러 개 지정 가능 (예: C:/Users/이름/Documents,D:/작업문서). 최초 실행 시 트레이 앱 설정 화면에서 사용자가 입력.",

  "RAG_EMBED_MODEL": "bge-m3",
  "RAG_EMBED_MODEL_PATH": "",
  "_RAG_EMBED_MODEL_PATH_설명": "bge-m3 GGUF 파일 경로. 배포판은 models\\ 폴더에 이 파일을 동봉하므로 보통 설치 경로 기준 상대경로로 런처가 자동 설정. 사용자가 이미 Ollama로 bge-m3를 받아둔 경우 그 경로를 재사용할 수도 있음(선택).",

  "RAG_EMBED_WORKERS": 4,
  "_RAG_EMBED_WORKERS_설명": "embed() 호출을 몇 개까지 동시에 큐잉하느냐일 뿐, CPU 코어 점유와는 무관함(실제 계산은 항상 직렬화됨) — 코어 점유를 줄이려면 이 값이 아니라 RAG_EMBED_THREADS를 낮출 것.",

  "RAG_EMBED_THREADS": 0,
  "_RAG_EMBED_THREADS_설명": "임베딩 계산(llama.cpp) 내부 스레드 수 — 실제 CPU 코어 점유를 결정하는 값. 0이면 코드 기본값(코어수의 1/4, 최소 1)을 씀. NotebookRAG는 상시 백그라운드 서비스라 코어 전체를 쓰지 않도록 일부러 1/4을 기본값으로 함(목표 CPU 사용률 약 25%) — 색인이 너무 느리면 이 값을 올리고, 컴퓨터가 무거워지면 낮출 것.",

  "ANTHROPIC_API_KEY": "",
  "_ANTHROPIC_API_KEY_설명": "선택 사항. MCP 전용 사용(Claude Code 등에서 doc_search로만 검색)이라면 비워둬도 됩니다 — 원문 청크 반환 경로는 API 키가 필요 없습니다. 카카오톡 같은 채널 연동으로 rag-ra가 직접 문장 답변을 만들게 하려면 입력하세요.",

  "RAG_HTTP_HOST": "127.0.0.1",
  "RAG_HTTP_PORT": 8420,
  "_RAG_HTTP_PORT_설명": "notebookrag_main.py(검색 /rag/* + /health, 그리고 /indexer/*는 아래 색인 프로세스로 중계)가 바인딩하는 로컬 HTTP 포트. 127.0.0.1에만 바인딩하며 외부 노출은 하지 않음. 트레이 앱 등 외부 클라이언트는 항상 이 포트만 알면 됨.",

  "INDEXER_PROC_PORT": 8421,
  "_INDEXER_PROC_PORT_설명": "[긴급 삭제안전장치] 색인(스캔/임베딩) 자식 프로세스가 자체적으로 바인딩하는 로컬 HTTP 포트. RAG_HTTP_PORT와 절대 겹치지 않아야 함. notebookrag_main.py의 /indexer/*는 이 포트로 요청을 그대로 중계(proxy)한다 — 예전엔 감시 폴더 설정을 파일(indexer_config.json)로 주고받다가 쓰는 도중 읽는 경쟁 상태로 기존 색인 데이터가 통째로 삭제되는 사고가 있었음. HTTP 요청/응답은 본질적으로 원자적이라 이 경쟁 상태 자체가 없다. 127.0.0.1에만 바인딩, 외부 노출 없음.",

  "IDLE_RESCAN_INTERVAL_SEC": 30,
  "_IDLE_RESCAN_INTERVAL_SEC_설명": "색인 루프가 한 회차를 끝낸 뒤 다음 회차 스캔을 시작하기까지 대기하는 시간(초). 트레이 앱에서 폴더를 추가하면 이 시간 이내에 반영됨.",

  "MODEL_DOWNLOAD_URL": "",
  "MODEL_SHA256": "",
  "_MODEL_DOWNLOAD_URL_설명": "RAG_EMBED_MODEL_PATH 자리에 모델 파일이 없을 때 notebookrag_main.py가 최초 실행 시 자동으로 받아올 bge-m3 GGUF 파일의 직접 다운로드 URL과 SHA256 체크섬. 정책상 이 값은 하드코딩하지 않고 비워둠 — src/model_downloader.py 상단 docstring에 조사로 확인한 실제 값(공개/비gated, MIT 라이선스, ggml-org/bge-m3-Q8_0-GGUF)이 적혀 있으니 그 값을 그대로 넣으면 됨. 둘 다 비어있고 모델 파일도 없으면 GET /model/status가 phase:\"error\"를 반환함(서버 자체는 안 죽음)."
}
